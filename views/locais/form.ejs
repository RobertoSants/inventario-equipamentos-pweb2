<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= titulo %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%= titulo %></h1>

    <form action="/locais" method="POST">
        
        <div class="form-group">
            <label for="nome">Nome do Local:</label>
            <input type="text" id="nome" name="nome" value="<%= dados.nome || '' %>" placeholder="Ex: Laboratório de Informática">
            <% if (erros.nome) { %>
                <div class="error"><%= erros.nome.msg %></div>
            <% } %>
        </div>

        <div class="form-group">
            <label for="bloco">Bloco:</label>
            <input type="text" id="bloco" name="bloco" value="<%= dados.bloco || '' %>" placeholder="Ex: Bloco A">
            <% if (erros.bloco) { %>
                <div class="error"><%= erros.bloco.msg %></div>
            <% } %>
        </div>

        <div class="form-group">
            <label for="sala">Sala:</label>
            <input type="text" id="sala" name="sala" value="<%= dados.sala || '' %>" placeholder="Ex: 101">
            <% if (erros.sala) { %>
                <div class="error"><%= erros.sala.msg %></div>
            <% } %>
        </div>

        <button type="submit">Salvar</button>
        <a href="/locais" style="margin-left: 10px;">Cancelar</a>
    </form>
</body>
</html>