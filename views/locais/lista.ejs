<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= titulo %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%= titulo %></h1>
    
    <a href="/locais/novo" class="btn">Cadastrar Novo Local</a>
    <a href="/" style="margin-left: 10px;">Voltar para Home</a>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Bloco</th>
                <th>Sala</th>
            </tr>
        </thead>
        <tbody>
            <% locais.forEach(local => { %>
                <tr>
                    <td><%= local.id %></td>
                    <td><%= local.nome %></td>
                    <td><%= local.bloco %></td>
                    <td><%= local.sala %></td>
                </tr>
            <% }) %>

            <% if (locais.length === 0) { %>
                <tr>
                    <td colspan="4" style="text-align: center;">Nenhum local cadastrado.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>